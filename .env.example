# Copy to .env and fill in (never commit .env).
# See docs/render-deploy.md

# Recommended: Render → property-launch-cal → Settings → Deploy Hook
RENDER_DEPLOY_HOOK_URL=

# Or use API (Account Settings → API Keys + service srv- id)
# RENDER_API_KEY=
# RENDER_SERVICE_ID=
